<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.myapp.tata_parikshan_2019">

    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera2" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_tata_steel"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_tata_steel"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".supervisior_key_login"
            android:parentActivityName=".MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity
            android:name=".Supervisior_trip_details"
            android:label="Trip Details"
            android:parentActivityName=".ScannerActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ScannerActivity" />
        </activity>
        <activity
            android:name=".supervisiorHome"
            android:label="Tata Parikshan" />
        <activity
            android:name=".ScannerActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".AdminActivity"
            android:label="Tata Parikshan"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SettingsActivity"
            android:label="Settings"
            android:parentActivityName=".AdminActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".AdminActivity" />
        </activity>
    </application>

</manifest>